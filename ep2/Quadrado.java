Class Quadrado(){
	Int perimetro, área;
	setPerimetro(){
		this.perimetro = 4*getLado();
}
	getPerimetro(){
		return this.perimetro;
}
	setArea(int lado)
		this.area = Math.pow(getLado(), 2);
}
	getArea(){
		return this.area;
}